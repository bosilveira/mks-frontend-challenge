import { StyledShop } from "./styled";

interface ShopInterface {
    data?: JSX.Element,
}

export default function Shop( { data }: ShopInterface) {
    return (
      <StyledShop>
        
      </StyledShop>
    );
  }